#include <stdlib.h> 
/*
int conv_char_wchar_utf8(char* s_str, wchar_t* e_str) {
    size_t     i;
    size_t     len;
    len = sizeof(s_str) / sizeof(char);
    wchar_t  *e_str = new wchar_t[1 + len];
    for( i = 0; s_str[i]; i++ )
        e_str[i] = s_str[i];
    e_str[i] = 0;
    return 0;
}
*/